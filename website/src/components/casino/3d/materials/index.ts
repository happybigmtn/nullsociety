/**
 * Materials Infrastructure - PBR presets and lighting exports
 */

export {
  type MaterialPreset,
  MATERIAL_PRESETS,
  createMaterialFromPreset,
  createPhysicalMaterialFromPreset,
  type EmissiveConfig,
  EMISSIVE_PRESETS,
  type LightingPreset,
  type LightingConfig,
  LIGHTING_PRESETS,
} from './MaterialConfig';
